import { ConvertviewPipe } from './convertview.pipe';

describe('ConvertviewPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertviewPipe();
    expect(pipe).toBeTruthy();
  });
});
