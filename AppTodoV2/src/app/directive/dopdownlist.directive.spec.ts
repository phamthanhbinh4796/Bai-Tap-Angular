import { DopdownlistDirective } from './dopdownlist.directive';

describe('DopdownlistDirective', () => {
  it('should create an instance', () => {
    const directive = new DopdownlistDirective();
    expect(directive).toBeTruthy();
  });
});
