import { SortmessagePipe } from './sortmessage.pipe';

describe('SortmessagePipe', () => {
  it('create an instance', () => {
    const pipe = new SortmessagePipe();
    expect(pipe).toBeTruthy();
  });
});
